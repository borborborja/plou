{
  "name": "plou",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "description": "Alarma de lluvia y previsión meteorológica basada en radar",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "start": "npm run start --workspace=server",
    "test": "npm run test --workspaces --if-present",
    "keys": "node server/scripts/generate-vapid.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
